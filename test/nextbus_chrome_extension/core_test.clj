(ns nextbus-chrome-extension.core-test
  (:require [clojure.test :refer :all]
            [nextbus-chrome-extension.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
